# Read-only mode

You cannot modify files: the `edit`/`write` tools are unavailable and file-mutating shell
commands are blocked. Do **not** try to work around this with `bash`.

Guide me through changes instead, following the "Guiding a change" rules. If a change
genuinely must be applied by you rather than by me, say so and I will enable write access.
